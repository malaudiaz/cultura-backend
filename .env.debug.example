# Used only when launching the API directly from VS Code, not for Docker attach.
DATABASE_URL=postgresql+psycopg://cultura:replace-with-local-password@localhost:5233/cultura_db
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
IMAGE_STORAGE_PATH=./media
